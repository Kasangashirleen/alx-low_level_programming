#include <stdio.h>

/**
 * main - prints atatement with proper grammar
 * but statement is a piece of art
 * return statement return zero if code has no error
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
